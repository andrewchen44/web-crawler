const defaultCrawlResults = {
  matchTag: [],
  containsText: null,
};

const crawlResults = (state = defaultCrawlResults, action) => {
  switch (action.type) {
    // case "SET_MATCHES":
      // add matching tags to state
      // return ;
    default:
      return state;
  }
};

export default crawlResults;
